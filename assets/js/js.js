
$('#goingtocarousel1').click(function () {
    if (x.matches) {
        scrollTo($('#caroseldown'), 3000);
    } else {
        scrollTo($('#caroseldown'), 2200);
    }
});
$('#goingtocarousel2').click(function () {
    if (x.matches) {
        scrollTo($('#caroseldown'), 3000);
    } else {
        scrollTo($('#caroseldown'), 2200);
    }
});
$('#goingtocarousel3').click(function () {
    if (x.matches) {
        scrollTo($('#caroseldown'), 3000);
    } else {
        scrollTo($('#caroseldown'), 2200);
    }
});
$('#goingtocarousel4').click(function () {
    if (x.matches) {
        scrollTo($('#caroseldown'), 3000);
    } else {
        scrollTo($('#caroseldown'), 2200);
    }
});
$('#goingtocarousel5').click(function () {
    if (x.matches) {
        scrollTo($('#caroseldown'), 3000);
    } else {
        scrollTo($('#caroseldown'), 2200);
    }
});
$('#goingtocarousel6').click(function () {
    if (x.matches) {
        scrollTo($('#caroseldown'), 3000);
    } else {
        scrollTo($('#caroseldown'), 2200);
    }
});

var x = window.matchMedia("(min-width: 1920px)");
